---
import ButtonLogin from './ui/ButtonLogin.astro'
import NavUserProfile from './NavUserProfile.astro'
import { getSession } from 'auth-astro/server';

const session = await getSession(Astro.request);
---

{session ? <NavUserProfile /> : <ButtonLogin /> }
